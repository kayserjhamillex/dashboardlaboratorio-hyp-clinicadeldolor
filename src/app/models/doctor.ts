import { Title } from '@angular/platform-browser';

export interface Doctor {
  id?: number,
  Name?: string,
  LastName?: string,
  MedicalSchoolNumber?: string,
  Email?: string,
  Password?: string,
  Photo?: string,
  Code?: string,
  Condition?: string,
}
