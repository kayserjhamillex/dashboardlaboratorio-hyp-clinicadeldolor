import { Title } from '@angular/platform-browser';

export interface ListLaboratoriodos {
  id?: number,
  Name?: string,
  Detail?: string,
  Plurality?: string,
  Price?: number,
  TipodosId?: number
}
