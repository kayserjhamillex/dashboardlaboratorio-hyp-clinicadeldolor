import { Title } from '@angular/platform-browser';

export interface ListLaboratoriocita {
  id?: number,
  Value?: boolean,
  Location?: string,
  Amount?: string,
  Condition?: string,
  CitaId?: number,
  LaboratoriodosId?: number
}
