import { Title } from '@angular/platform-browser';
// import { Time } from '@angular/common';

export interface Mensaje {
  id?: number,
  SendDate?: Date,
  Message?: string,
  Direction?: string,
  ChatId?: number,
}
