import { Title } from '@angular/platform-browser';

export interface Empresa {
  id?: number,
  Name?: string,
  BusinessName?: string,
  Address?: string,
  Email?: string,
  Phone?: string,
  Phone2?: string,
  Logo?: string,
  History?: string,
  Mision?: string,
  Vision?: string
}
