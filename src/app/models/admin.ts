import { Title } from '@angular/platform-browser';

export interface Admin {
  id?: number,
  Name?: string,
  LastName?: string,
  Phone?: string,
  Email?: string,
  Password?: string,
  Condition?: string,
  ConditionMin?: string,
  Photo?: string,
  Code?: string
}
